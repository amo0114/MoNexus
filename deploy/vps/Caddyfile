{
  email {$CADDY_EMAIL}
}

{$CADDY_DOMAIN} {
  encode zstd gzip

  # The bundled Nginx image serves the SPA, proxies /api to Express, and
  # proxies /uploads to private MinIO storage.
  reverse_proxy web:80
}
