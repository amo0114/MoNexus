# CMI QA 证据映射表(cmi-qa-evidence-map)

| 字段 | 值 |
| --- | --- |
| 日期 | 2026-08-14 |
| 依据 | [AMD-CMI-012](../superpowers/specs/2026-08-13-cmi-qa-rescope-v012.md) §2/§4 证据规则;CHK-CATALOG-OPS-001 / CHK-MERCH-001 v0.1.2 |
| 采集方式 | 协调者 + 两个只读检索代理逐条比对;所有引用来自真实测试文件的 grep/读取,禁止臆造 |
| 状态语义 | covered=有直接断言;partial=部分断言缺失(附缺口);GAP=无证据;P1/Deferred/runbook=按修订不阻塞;C5-审计=Final Gate 出具 |
| 终局 | 本表是 C5 证据回填的输入;"当前 HEAD 证据"以 merge develop 后最终 HEAD 的 verify 运行为准 |

## 0. 协调者裁决(处置一览)

### 修复/补测(C3b,pi 实施)

| 项 | CHK | 内容 |
| --- | --- | --- |
| C3b-1 | XBD-003 | 无封面 confirm → 400 COVER_INVALID 零写入用例 |
| C3b-2 | XBD-002/SEC-002 | preview/confirm 响应与审计不含 objectKey/providerRef 缺失性断言 |
| C3b-3 | XBD-008 | `externalIntegration+externalSku` DB unique 生效用例 |
| C3b-4 | XBD-009 | 并发冲突 409 body 携带 `existingProductId` 断言 |
| C3b-5 | CAT-003 | 持 admin 角色但无 MFA 声明的 token → 403 MFA_REQUIRED |
| C3b-6 | CAT-010 | 分类改名不改历史 `Product.type`/订单快照 |
| C3b-7 | CAT-012 | **产品修复**:categoryService label/status 变更同时 `bumpProductListVersionCoalesced()` + 用例 |
| C3b-8 | PUBLIC-002 | `computeSponsoredBucket`/`computeRotationRank` 确定性与轮换公平性纯单测 |
| C3b-9 | PROMO-010 / EDIT-001 / SEC-003(MFA) / QA-004 | merchant 令牌打 admin merch 端点 403;无 MFA admin 被 `requireAdminMfa` 拒 |
| C3b-10 | PROMO-009 | `points: 0` 调整写不可变决定(CAS/同 key 重放/新 key 409)且零 refund PointLog |
| C3b-11 | PROMO-012 | (可选)禁词表扩展:法币计费/CPM/CPC/竞价/收益承诺 |
| C3b-12 | — | `scripts/verify-catalog-ops-e2e.sh` 固化已验证运行序列 |

### 特别裁决

- CHK-PROD-008 → covered:foundation Gate 3(conservation/statuses/publishedAt)即证据,C5 复跑出具;
- CHK-OPS-001/002(catalog)→ P1:catalog 指标未实现,登记观测性增量,PR 描述披露;
- CHK-UI-009 → C5-审计:git 祖先/文件所有权审计属 Final Gate;
- CHK-CAT-012 → C3b-7 修复:缓存陈旧窗口受 TTL 限制,但仍按冻结清单补齐主动失效。

### accepted-partial / P1 登记(不阻塞合并,PR 描述披露)

- **catalog**:XBD-004(越界 cover.mode 严格联合半边;无同步 AI 路径为结构性事实)、SEC-003(SSRF 定向断言——已有通用出网护栏 + strict schema 兜底)、SEC-004(stack/远端正文脱敏——errorHandler 结构性保证 + 稳定错误码已证)、UI-001(向导失败保输入)、UI-007(切分类缓存重置——latestRequest 通用件已证)、PERF-001~003(AMD-CMI-012 §2.5)。
- **merch**:HOT-011(`buildProductListCacheKey` 单测——run-pinned 行为已有集成证据)、SEC-004(日志脱敏)、OPS-001/002(charge/transition/public/entitlement 指标与 editorial/entitlements cron 幂等)、UI-006(对比度/缩放——axe 已跑但禁用 color-contrast 规则)、QA-006 visual 部分(随 ASSET Deferred)、PERF-002(P1)。

---

## 1. Catalog(CHK-CATALOG-OPS-001 §3–8)

| CHK-ID | 状态 | 证据 |
| --- | --- | --- |
| CHK-CAT-001 | covered | `server/src/modules/catalog/categoryService.test.ts → 'creates an active category with normalized label, canonical uniqueness and AdminLog'`<br>`categoryService.test.ts → 'rejects a reused code (409, code is never reused even after deactivation)'`<br>`categoryService.test.ts → 'paginates, filters by status and orders by (sortOrder, id)'` |
| CHK-CAT-002 | covered | `server/src/modules/catalog/categoryService.test.ts → 'public registry returns only active categories with deprecated productTypes'`<br>`categoryAdminRoutes.test.ts → 'returns active productCategories and deprecated productTypes from the DB'` |
| CHK-CAT-003 | partial | 鉴权与 AdminLog 已证：`categoryAdminRoutes.test.ts → 'creates, lists, updates, deactivates, activates, reorders and deletes'`、`'rejects unauthenticated callers (401)'`、`'rejects non-admin callers (403)'`、`categoryService.test.ts → 'transitions active↔inactive and is idempotent without redundant logs'`；**缺 MFA**：`requireAdminMfa` 仅在 `server/src/modules/admin/routes.ts:42` 结构性继承，无任何用例用「有 admin 角色但缺 MFA 声明的 token」打 `/api/admin/product-categories` 断言 403 MFA_REQUIRED |
| CHK-CAT-004 | covered | `categoryService.test.ts → 'refuses to delete a category referenced by a product (409)'`<br>`categoryService.test.ts → 'tombstones an unreferenced category: row kept inactive, code reserved forever, AdminLog written'`<br>`e2e/catalog-category-governance.spec.ts → 'admin cannot delete a category referenced by approved applications'` |
| CHK-CAT-005 | covered | `categorySchema.test.ts → 'is strict: status/isHot/deliveryMode cannot be smuggled in'`<br>`src/components/catalog/ProductCategorySelect.test.tsx → 'keeps taxonomy-only semantics: never switches deliveryMode (D-CAT-05)'`<br>`src/pages/merchant/ProductCreateWizard.test.tsx → 'selecting a category never switches the delivery mode (D-CAT-05 orthogonality)'` |
| CHK-CAT-006 | covered | `applicationService.test.ts → 'merchant list only returns the caller\'s own applications'`<br>`applicationService.test.ts → 'withdrawing another merchant\'s application returns 404 (no existence leak)'`<br>`applicationRoutes.test.ts → 'isolates merchants: cannot see or withdraw another merchant\'s application'` |
| CHK-CAT-007 | covered | `applicationService.test.ts → 'rejects a second pending application for the same merchant + normalizedLabel (409 stable code)'`<br>`applicationService.test.ts → 'allows a different normalizedLabel for the same merchant, and the same label for another merchant'`<br>`applicationRoutes.test.ts → 'rejects a duplicate pending application with the stable 409 code'` |
| CHK-CAT-008 | covered | `applicationService.test.ts → 'creates the Category + AdminLog atomically and bumps the public registry'`<br>`applicationService.test.ts → 'links an existing ACTIVE category without creating a duplicate'`<br>`applicationService.test.ts → 'rejects a pending application with reason, null resolution and structural AdminLog'` |
| CHK-CAT-009 | covered | `applicationService.test.ts → 'two concurrent approvals leave exactly one winner and one Category'`<br>`applicationService.test.ts → 'refuses a second review with a stable code and never creates a duplicate Category'`<br>`e2e/catalog-category-governance.spec.ts → 'merchant withdraws a pending application while the admin stale review dialog is open → 409 CATEGORY_APPLICATION_ALREADY_REVIEWED (D-CAT-10/AC-CAT-012)'` |
| CHK-CAT-010 | partial | 仅有 `categoryService.test.ts → 'renames label and updates normalizedLabel without touching immutable code'`，只证 code 不变；**缺**改名后回读历史 Product.type / 订单快照仍为旧 label 的断言 |
| CHK-CAT-011 | covered | `publicationReadiness.test.ts → 'allows an inactive category on republish (publishedAt already set) — D-CAT-22'`<br>`publicationReadiness.test.ts → 'fails with a stable CATEGORY_INACTIVE code on first publish into an inactive category'`<br>`server/src/modules/products/catalog-merch-integration.test.ts → 'keeps an inactive historical category in the DTO but excludes it from an active code filter'` |
| CHK-CAT-012 | partial | registry 侧已证：`applicationService.test.ts → 'creates the Category + AdminLog atomically and bumps the public registry'`；**缺 product list cache**：`categoryService.ts` 五处只调 `bumpCategoryRegistryCacheVersion`，`invalidateProductPublicCache` 只在 `productPublication.ts:98/146` 被调用，无用例证明分类变更后商品列表缓存失效 **处置:C3b-7 修复**(categoryService 变更同时 bump 产品列表缓存版本 + 用例) |
| CHK-PROD-001 | covered | `publicationRoutes.test.ts → 'creates a draft, publishes atomically, unpublishes, and preserves first publishedAt'`<br>`server/src/modules/merchant/instant-fixed-product.test.ts → 'creates limited instant_fixed drafts with zero initial capacity'`<br>`server/src/__tests__/admin-product-audit.test.ts → 'writes safe create and update audit entries in the same request path'` |
| CHK-PROD-002 | covered | `publicationRoutes.test.ts → 'enforces merchant ownership and strict draft request allowlists'`<br>`server/src/modules/merchant/product-is-hot.test.ts → 'create with top-level isHot returns 400 FIELD_NOT_WRITABLE and creates nothing'`<br>`src/api/catalog.test.ts → 'never leaks secret inventory / isHot / stock / unknown keys into the payload'` |
| CHK-PROD-003 | covered | `publicationReadiness.test.ts → 'is ready for a complete draft with cover, active category and a sellable offer'`<br>`publicationReadiness.test.ts → 'collects every missing condition at once with stable codes'`<br>`publicationReadiness.test.ts → 'is ready when one active offer is sellable even if another active offer is empty but config-valid'` |
| CHK-PROD-004 | covered | `publicationRoutes.test.ts → 'returns stable readiness details and leaves an incomplete draft unchanged'`<br>`publicationReadiness.test.ts → 'fails with a stable COVER_REQUIRED code when no cover is set'`<br>`src/api/catalog.test.ts → 'extracts stable issues from a PRODUCT_NOT_READY payload'` |
| CHK-PROD-005 | covered | `publicationRoutes.test.ts → 'creates a draft, publishes atomically, unpublishes, and preserves first publishedAt'`<br>`publicationReadiness.test.ts → 'never performs remote I/O: only findUnique/count on the injected client are called'`<br>`e2e/catalog-product-lifecycle.spec.ts → 'merchant publishes the product, then the public detail serves the topped-up offer'` |
| CHK-PROD-006 | covered | `e2e/catalog-product-lifecycle.spec.ts → 'after unpublish, offer capacity is preserved and the merchant republishes from the UI'`<br>`publicationRoutes.test.ts → 'creates a draft, publishes atomically, unpublishes, and preserves first publishedAt'`<br>`e2e/catalog-product-lifecycle.spec.ts → 'merchant unpublishes the product from the dashboard UI'` |
| CHK-PROD-007 | covered | `e2e/catalog-product-lifecycle.spec.ts → 'draft is rejected by public detail and checkout preview'`<br>`e2e/catalog-product-lifecycle.spec.ts → 'after unpublish, public detail, store search and checkout preview reject the product'`（服务端仅 `products/service.ts:59/94/510` 的 `status:'active'` 过滤，无独立集成用例，按 AMD-CMI-012 §2 走关键旅程浏览器证据） |
| CHK-PROD-008 | covered | `scripts/verify-catalog-foundation.sh` Gate 3(legacy-clean upgrade + conservation/statuses/publishedAt 验证)即本项证据;C5 最终 Gate 复跑出具。**协调者覆盖原 GAP 判定** |
| CHK-PROD-009 | covered | `resolver.test.ts → 'rejects when both categoryId and legacy type are supplied (LEGACY_TYPE_WITH_CATEGORY_ID)'`<br>`resolver.test.ts → 'maps an exact legacy type to the active category with that label'`<br>`resolver.test.ts → 'rejects an unknown legacy type instead of falling back to legacy-unclassified'` |
| CHK-PROD-010 | covered | `server/src/__tests__/admin-product-audit.test.ts → 'writes safe create and update audit entries in the same request path'`<br>`server/src/modules/catalog/fakaPreviewConfirm.test.ts → 'confirm creates one platform draft/link atomically and same key replays it'`<br>`src/api/admin.catalog.test.ts → 'creates a platform draft without exposing merchant/hot/stock controls'` |
| CHK-INV-001 | covered | `server/src/modules/merchant/inventory.test.ts → 'voids only the selected offer inventory, leaving the default offer pool intact'`<br>`src/api/merchant.offer-first.test.ts → 'uses explicit Offer IDs in every inventory/capacity URL and never sends offerId in the body'`<br>`server/src/__tests__/admin.test.ts → 'falls back to the sole instant_inventory offer when the default offer is manual_service'` |
| CHK-INV-002 | covered | `server/src/modules/merchant/capacity-adjust.test.ts → 'rejects capacity adjustment for immediate inventory and unlimited products'`<br>`server/src/modules/merchant/instant-fixed-product.test.ts → 'rejects direct stock update for instant_inventory products'` |
| CHK-INV-003 | covered | `server/src/modules/merchant/instant-fixed-product.test.ts → 'rejects inventory import for instant_fixed products'`<br>`instant-fixed-product.test.ts → 'rejects admin inventory import for instant_fixed products'`<br>`server/src/__tests__/admin.test.ts → 'rejects non-instant offers, foreign offers, templated offers, and products without instant offers'` |
| CHK-INV-004 | covered | `server/src/modules/merchant/inventory.test.ts → 'previews without writes, re-analyses on confirm, and exposes offer-scoped audit without content'`<br>`inventory.test.ts → 'allows admin preview/confirm on the same offer contract and records offer in both audit trails'`<br>`src/components/catalog/AdminCatalogWorkflows.test.tsx → 'requires explicit Offer preview before admin inventory confirm'` |
| CHK-INV-005 | covered | `server/src/modules/merchant/inventory.test.ts → 'does not write logs when import is rejected'`<br>`inventory.test.ts → 'rejects imports beyond the bounded row limit before writing anything'`<br>`server/src/__tests__/admin.test.ts → 'normalizes blank lines, rejects duplicate or existing items, and never partially writes'` |
| CHK-INV-006 | covered | `server/src/modules/merchant/inventory.test.ts → 'keeps limited capacity non-negative under 50 concurrent decrements'`<br>`server/src/modules/merchant/capacity-adjust.test.ts → 'never permits a concurrent-safe decrement below the remaining capacity'`<br>`capacity-adjust.test.ts → 'adjusts fixed-content capacity atomically and records an attributed reason'` |
| CHK-INV-007 | covered | `server/src/modules/merchant/inventory.test.ts → 'voids earliest available items, derives remaining stock from items and writes a void log'`<br>`inventory.test.ts → 'rejects when count exceeds available items without any change'`<br>`inventory.test.ts → 'never voids sold items'` |
| CHK-INV-008 | covered | `server/src/modules/merchant/inventory.test.ts → 'voids only the URL offer and returns both offer and product availability totals'`<br>`inventory.test.ts → 'projects available inventory separately for every Offer and the Product aggregate'` |
| CHK-INV-009 | covered | `server/src/modules/merchant/inventory.test.ts → 'previews without writes, re-analyses on confirm, and exposes offer-scoped audit without content'`<br>`inventory.test.ts → 'includes a sale and its order reference without ever returning the delivery content'`<br>`inventory.test.ts → 'returns paginated logs in reverse chronological order without inventory content'` |
| CHK-XBD-001 | covered | `server/src/modules/catalog/fakaPreviewConfirm.test.ts → 'preview is zero-write and returns sanitized normalized data'`<br>`externalCatalog.test.ts → 'normalizes source metadata and never stores raw HTML in sourceSnapshot'`<br>`externalCatalog.test.ts → 'sorts object keys recursively while preserving array order'` |
| CHK-XBD-002 | partial | active StoredObject 半边已证：`fakaPreviewConfirm.test.ts → 'accepts only an active registered uploaded cover and snapshots its URL'`；**缺**响应不暴露 objectKey/providerRef 的断言（全仓无 `objectKey` 缺失性断言） |
| CHK-XBD-003 | partial | 默认封面快照半边已证：`fakaPreviewConfirm.test.ts → 'confirm creates one platform draft/link atomically and same key replays it'`（imageUrl/images = 分类 defaultCoverUrl）；FE 侧 `src/components/catalog/AdminCatalogWorkflows.test.tsx → 'disables Xboard confirm when preview has no valid cover'`（mock）；**缺**服务端「分类无 defaultCoverUrl 时 confirm 被拒」用例，实际行为 400 COVER_INVALID 未验证 |
| CHK-XBD-004 | partial | `categorySchema.test.ts → 'rejects absolute, protocol-relative and relative URLs'`<br>`contentSanitizer.test.ts → 'keeps the frozen formatting allowlist and removes executable/remote content'`；**缺**：cover 严格联合类型（`server/src/modules/admin/schema.ts:198-202` 仅 `uploaded`/`category_default`）无越界 mode 被拒的用例，且无用例断言不存在同步 AI 调用路径 |
| CHK-XBD-005 | covered | `server/src/modules/catalog/contentSanitizer.test.ts → 'keeps the frozen formatting allowlist and removes executable/remote content'`<br>`e2e/catalog-xboard-import.spec.ts → 'admin previews and confirms a sanitized Xboard draft via the real UI'`（断言 `admin-faka-rich-preview` 的 innerHTML 无 script/img/onerror/javascript:） |
| CHK-XBD-006 | covered | `fakaPreviewConfirm.test.ts → 'rejects a changed source and writes nothing'`<br>`fakaPreviewConfirm.test.ts → 'provider failure during confirm leaves every business table untouched'`<br>`e2e/catalog-xboard-import.spec.ts → 'admin rejects a stale Xboard preview with zero product writes'` |
| CHK-XBD-007 | covered | `fakaPreviewConfirm.test.ts → 'confirm creates one platform draft/link atomically and same key replays it'`<br>`e2e/catalog-xboard-import.spec.ts → 'admin replays the same Xboard request and rejects idempotency key reuse'`<br>`externalCatalog.test.ts → 'freezes request hashes and validates idempotency keys'` |
| CHK-XBD-008 | partial | `fakaPreviewConfirm.test.ts → 'uses database uniqueness for concurrent different-key confirms'` 证明 `@@unique([provider, externalProductId])`（schema.prisma:1253）在 DB 层生效；**缺** `@@unique([externalIntegration, externalSku])`（schema.prisma:566）的 DB 层用例 |
| CHK-XBD-009 | partial | 最终唯一性已证：`fakaPreviewConfirm.test.ts → 'uses database uniqueness for concurrent different-key confirms'`（201/409 各一、Product 计数=1）；existingProductId 仅有 mock 级 FE 证据 `AdminCatalogWorkflows.test.tsx → 'surfaces the existing Product returned by an external identity conflict'`；**缺**服务端断言 409 body 携带 `existingProductId` |
| CHK-XBD-010 | covered | `fakaPreviewConfirm.test.ts → 'provider failure during confirm leaves every business table untouched'`<br>`fakaPreviewConfirm.test.ts → 'confirm creates one platform draft/link atomically and same key replays it'`<br>`fakaPreviewConfirm.test.ts → 'rejects a changed source and writes nothing'` |
| CHK-UI-001 | partial | `src/pages/merchant/ProductCreateWizard.test.tsx → 'saves a draft with a categoryId-based whitelist payload and retains productId'`<br>`ProductCreateWizard.test.tsx → 'is idempotent: re-submitting an already-saved draft never creates a second one'`；**缺**「保存失败后向导输入不丢失」的断言（现有失败重试用例 `src/pages/MerchantDashboardPage.test.tsx → '失败不伪造成功（只显示错误），按钮恢复可 retry，retry 成功并刷新'` 覆盖的是 dashboard 上下架按钮，不是向导表单） |
| CHK-UI-002 | covered | `src/components/catalog/ProductCategorySelect.test.tsx → 'keeps taxonomy-only semantics: never switches deliveryMode (D-CAT-05)'`<br>`src/pages/merchant/ProductCreateWizard.test.tsx → 'selecting a category never switches the delivery mode (D-CAT-05 orthogonality)'`<br>`src/api/catalog.test.ts → 'never leaks secret inventory / isHot / stock / unknown keys into the payload'` |
| CHK-UI-003 | covered | `src/components/catalog/ProductPublicationChecklist.test.tsx → 'lists every readiness issue with its stable code and human copy'`<br>`ProductPublicationChecklist.test.tsx → 'gates the publish CTA on ready and keeps it keyboard operable when allowed'`<br>`ProductPublicationChecklist.test.tsx → 'renders the stable code for tooling even though it is visually hidden'` |
| CHK-UI-004 | covered | `src/components/catalog/ProductAvailabilityStep.test.tsx → 'exposes Offer-first selection with the mutual-exclusion action word on each option'`<br>`src/components/merchant/MerchantAvailabilityModal.test.tsx → 'selects an Offer before revealing its mutually exclusive action and splits Offer/Product totals'`<br>`src/components/merchant/MerchantAvailabilityModal.test.tsx → 'keeps preview before confirm and targets the already-selected inventory Offer'` |
| CHK-UI-005 | covered | `src/components/catalog/AdminCategoryManager.test.tsx → 'renders inactive historical semantics and keeps independent list filters'`<br>`AdminCategoryManager.test.tsx → 'uses a confirmation step and disables duplicate category mutations'`<br>`src/components/catalog/CategoryApplicationPanel.test.tsx → 'validates create input and prevents a second submit while the request is in flight'` |
| CHK-UI-006 | covered | `src/pages/AdminPage.test.tsx → 'renders the catalog governance nav and mounts the isolated panel without extra host API requests'`<br>`AdminPage.test.tsx → 'renders the merchandising nav and mounts the isolated page without extra host API requests'`<br>`src/pages/MerchantDashboardPage.test.tsx → '分类申请 tab：渲染 CategoryApplicationPanel 且不触发 merchant 列表 mock'` |
| CHK-UI-007 | partial | 参数切换已证：`src/pages/StorePage.cmi.test.tsx → 'A: dynamic productCategories drive categoryCode on organic GET /products'`；陈旧响应压制有通用件 `src/utils/latestRequest.test.ts → 'prevents an older background response from overwriting a newer refresh'`；**缺**切换分类后 list/cursor/scroll 缓存被重置、旧页结果不混入的断言 |
| CHK-UI-008 | covered | `server/src/modules/products/catalog-merch-integration.test.ts → 'returns category DTOs, prefers categoryCode and keeps the legacy label filter compatible'`<br>`src/pages/StorePage.cmi.test.tsx → 'B: legacy productTypes fall back to category param and render a category/merchandising-less DTO without crashing'`<br>`StorePage.cmi.test.tsx → 'A: dynamic productCategories drive categoryCode on organic GET /products'` |
| CHK-UI-009 | C5-审计 | git 祖先/文件所有权审计(`git merge-base --is-ancestor` + diff owner),属 C5 Final Gate 出具,非自动化测试 |
| CHK-SEC-001 | covered | `publicationRoutes.test.ts → 'enforces merchant ownership and strict draft request allowlists'`<br>`applicationRoutes.test.ts → 'rejects non-merchant callers on merchant routes (403)'`<br>`server/src/modules/merchant/inventory.test.ts → 'rejects a URL offer that belongs to a different product on every merchant operation'` |
| CHK-SEC-002 | partial | 库存 secret / credential / 危险 HTML 三项已证：`inventory.test.ts → 'previews without writes, re-analyses on confirm, and exposes offer-scoped audit without content'`、`fakaPreviewConfirm.test.ts → 'preview is zero-write and returns sanitized normalized data'`、`contentSanitizer.test.ts → 'keeps the frozen formatting allowlist and removes executable/remote content'`；**缺** object key 不外泄的断言（与 CHK-XBD-002 同一缺口） |
| CHK-SEC-003 | partial | 通用出网护栏已证：`server/src/__tests__/faka-bridge-config.test.ts → 'refuses http FAKA_BRIDGE_URL in production'`、`'refuses FAKA_BRIDGE_ALLOW_INSECURE_TARGETS in production'`、`storage-endpoint-guard.test.ts → 'rejects link-local metadata range'`；**缺**针对 Xboard catalog 路径本身的断言：无用例证明 preview/confirm 的目标 URL 完全由服务端配置决定、请求体无任何字段可影响出网目标 |
| CHK-SEC-004 | partial | 稳定错误码（不暴露约束名）已证：`categoryService.test.ts → 'rejects a reused code (409, code is never reused even after deactivation)'`、`applicationService.test.ts → 'rolls back to pending when the proposed code is already taken (409 CATEGORY_CODE_TAKEN)'`；`server/src/middlewares/errorHandler.ts` 结构上 500 只回固定文案；**缺**断言 catalog 错误响应不含 stack、且 provider 失败的 503 body 不含远端响应正文 |
| CHK-PERF-001 | P1 | `P1 (AMD-CMI-012 §4.1)` |
| CHK-PERF-002 | P1 | `P1 (AMD-CMI-012 §4.1)` |
| CHK-PERF-003 | P1 | `P1 (AMD-CMI-012 §4.1)` |
| CHK-OPS-001 | P1 | catalog 侧指标(legacy type usage / publish failure code / import replay-conflict)未实现——登记 P1 观测性增量,PR 描述披露。**协调者覆盖原 GAP 判定** |
| CHK-OPS-002 | P1 | 依赖 CHK-OPS-001;随 P1 观测性增量一并交付 |

## GAP 清单

| CHK-ID | 状态 | 缺失的断言（一句话） |
| --- | --- | --- |
| CHK-CAT-003 | partial | 缺「持 admin 角色但无 MFA 声明的 token 打 `/api/admin/product-categories` 返回 403 MFA_REQUIRED」的用例，MFA 边界目前只靠 `server/src/modules/admin/routes.ts:42` 的结构性继承。 |
| CHK-CAT-010 | partial | 缺「重命名分类 label 后，既有 Product.type 与订单快照仍为旧 label」的回读断言。 |
| CHK-CAT-012 | partial | 缺「分类 mutation 后商品列表缓存失效」的断言——`categoryService.ts` 只 bump registry 版本，从不调用 `invalidateProductPublicCache`。 |
| CHK-PROD-008 | GAP | 缺「legacy fixture 升级后原 active 商品未被自动下架」的迁移级用例。 |
| CHK-XBD-002 | partial | 缺「preview/confirm 响应不含 StoredObject.objectKey / providerRef」的缺失性断言。 |
| CHK-XBD-003 | partial | 缺「分类无 defaultCoverUrl 时 confirm 被拒」的服务端用例（实际返回 400 COVER_INVALID，未验证）；默认封面快照半边已由 `fakaPreviewConfirm.test.ts` 覆盖。 |
| CHK-XBD-004 | partial | 缺「越界 cover.mode（如 remote_url）被严格联合类型拒绝」以及「不存在同步 AI 调用路径」的断言。 |
| CHK-XBD-008 | partial | 缺 `@@unique([externalIntegration, externalSku])`（schema.prisma:566）在数据库层生效的用例。 |
| CHK-XBD-009 | partial | 缺「服务端 409 冲突响应 body 携带 existingProductId」的断言，现仅有 FE mock 级证据。 |
| CHK-UI-001 | partial | 缺「草稿保存失败后向导已填输入不丢失」的组件级断言。 |
| CHK-UI-007 | partial | 缺「切换分类 code 后 list/cursor/scroll 缓存被重置、旧页结果不混入」的断言。 |
| CHK-UI-009 | GAP | 属 git 祖先与文件所有权审计，无自动化测试可证，须在 Final Gate 以 `git merge-base --is-ancestor` + diff owner 审计出具。 |
| CHK-SEC-002 | partial | 缺 object key 不出现在 API/log/AdminLog/trace 的断言（与 CHK-XBD-002 同源）。 |
| CHK-SEC-003 | partial | 缺「Xboard preview/confirm 的出网目标完全由服务端配置决定、请求体无字段可影响目标」的针对性断言。 |
| CHK-SEC-004 | partial | 缺「catalog 错误响应不含 stack、provider 失败 503 body 不含远端响应正文」的断言。 |
| CHK-OPS-001 | GAP | legacy type usage / publish failure code / import replay-conflict 三类有界指标尚未实现，无任何 catalog 指标可断言。 |
| CHK-OPS-002 | GAP | 依赖 OPS-001；指标不存在，无法断言不含 merchant/product/email/content/object key 高基数标签。 |

---

## 2. Merch(CHK-MERCH-001 §2–7)

| CHK-ID | 状态 | 证据 |
| --- | --- | --- |
| CHK-HOT-001 | covered | `server/src/modules/merchant/product-is-hot.test.ts` → `create with top-level isHot returns 400 FIELD_NOT_WRITABLE and creates nothing`<br>`server/src/modules/admin/product-is-hot.test.ts` → `update with top-level isHot returns 400 FIELD_NOT_WRITABLE and preserves prior isHot`<br>`src/components/merchant/MerchantProductFormModal.test.tsx` → `submits a categoryId-based whitelist payload with no type/isHot/stock` |
| CHK-HOT-002 | covered | `server/src/modules/merchandising/__tests__/ranking-compute-projection.test.ts` → `never reads Product/Offer.sales or legacy Product.isHot (CHK-HOT-002 / AC-MERCH-003)`<br>同文件 → `enforces the half-open window, status/refund scope, active population and stable tie-break`（真实 PG，含 sales=999/isHot=true 诱饵行） |
| CHK-HOT-003 | covered | `ranking-compute-projection.test.ts` → `renders the half-open window, refund exclusion and active population (SPEC-MERCH-001 §6.1)`（断言 `<> 'refunded'`，pending/processing/delivered/closed/disputed 全计入）<br>同文件 → `enforces the half-open window, status/refund scope, active population and stable tie-break` |
| CHK-HOT-004 | covered | `ranking-lifecycle.test.ts` → `loadRankingConfig returns frozen merchandising SystemConfig values`<br>同文件 → `validateRankingConfig rejects out-of-range frozen values (pure, no DB)`<br>同文件 → `creates running (short tx A), writes snapshots + completes (tx B), freezes config and DB time` |
| CHK-HOT-005 | covered | `ranking-compute-projection.test.ts` → `AC-MERCH-004: 10 active products, 20%/min5 → only top 2 (each >=5) are hot`<br>同文件 → `tie is stable by productId DESC (D-MERCH-05)`<br>同文件 → `minSales gate: rank within cutoff but below minSales is not hot (D-MERCH-05)` |
| CHK-HOT-006 | covered | `ranking-lifecycle.test.ts` → `two concurrent lifecycle runs produce exactly one run row`<br>同文件 → `findLatestCompletedRun only ever returns the latest completed run`（running/failed 不公开）<br>同文件 → `reclaims kill -9 leftover stale running by DB time as RUN_TIMEOUT; fresh running survives` |
| CHK-HOT-007 | covered | `ranking-lifecycle.test.ts` → `injected compute failure → run failed, no partial snapshot, previous completed still readable`<br>同文件 → `commit failure (CHECK violation in snapshot write) → whole tx B rolled back, run failed, previous intact`<br>同文件 → `mark-failed terminal CAS collision never fabricates wrap-up success` |
| CHK-HOT-008 | covered | `ranking-compute-projection.test.ts` → `renders the half-open window, refund exclusion and active population (SPEC-MERCH-001 §6.1)`（refunded 排除 + 仅 active population）<br>`ranking-lifecycle.test.ts` → `runRankingRun reclaims stale running then completes a new run`（每 run 全量重算即收敛） |
| CHK-HOT-009 | covered | `ranking-compute-projection.test.ts` → `run=null → no-run fallback projection, hot block absent, never legacy isHot (AC-MERCH-008)`<br>`server/src/modules/products/catalog-merch-integration.test.ts` → `uses id DESC with hot=null before the first run and never exposes legacy isHot` |
| CHK-HOT-010 | covered | `ranking-lifecycle.test.ts` → `cleans superseded completed runs (>48h, keeps latest), failed snapshots (>48h) and failed runs (>7d)`<br>`ranking-compute-projection.test.ts` → `pinned run cleaned by retention or id mismatch → 409 (AC-MERCH-007)`<br>同文件 → `recognizable old/unknown cursor version → 409 PRODUCT_CURSOR_EXPIRED (D-MERCH-07)` |
| CHK-HOT-011 | partial | `ranking-compute-projection.test.ts` → `computeFilterHash matches the frozen SHA-256 vectors (CHK-HOT-011)`<br>同文件 → `assertCursorFilterMatches accepts the matching filter and 409s on mismatch (SPEC-MERCH-001 §6.3)`<br>`catalog-merch-integration.test.ts` → `orders by a completed run and keeps the cursor pinned when a newer run completes`；但 `server/src/modules/products/cache.ts` 的 `buildProductListCacheKey`（含 `rankingRunId`）无任何测试引用 |
| CHK-HOT-012 | covered | `ranking-lifecycle.test.ts` → `listAdminRuns is paginated, ordered, and exposes only desensitized run fields + snapshot count`<br>`src/components/merchandising/AdminMerchandisingRunPanel.test.tsx` → `shows loading, then renders the initial list with the exact query and no order/user details`<br>同文件 → `does not fake success when recompute resolves failed: alert plus failed run details` |
| CHK-PROMO-001 | covered | `promotions-campaign.test.ts` → `create: price/placement/duration come from server package snapshot; pending does NOT charge`<br>同文件 → `client cannot override price/placement/duration (unknown body fields are not accepted)`<br>同文件 → `admin create → code immutable: updatePackage cannot change code` |
| CHK-PROMO-002 | covered | `promotions-campaign.test.ts` → `merchant cannot request another merchant’s product (404, no existence leak)`<br>同文件 → `merchant list is isolated: merchant B cannot see merchant A’s campaigns`<br>同文件 → `merchant cannot cancel another merchant’s campaign (404)` |
| CHK-PROMO-003 | covered | `promotions-campaign.test.ts` → `merchant cancels own pending_review → cancelled, uncharged; repeat cancel idempotent`<br>同文件 → `merchant cannot cancel a rejected campaign (409 CAMPAIGN_TRANSITION_INVALID)`<br>`promotions-dto-state.test.ts` → `pending_review is uncharged (no PointLog / balance mutation at request time)` |
| CHK-PROMO-004 | covered | `promotions-billing.test.ts` → `100 concurrent approvals charge exactly once and never make balance negative`（断言 balance、PointLog out=1、AdminLog=1、chargePointLogId 非空） |
| CHK-PROMO-005 | covered | `promotions-billing.test.ts` → `insufficient balance records payment_failed with zero ledger rows, then retry charges the approved snapshot`（retry 用 approve 时快照价，套餐改价/停用不影响） |
| CHK-PROMO-006 | covered | `promotions-billing.test.ts` → `concurrent approval of two pending rows relies on the placement constraint and rolls one charge back`<br>`promotions-campaign.test.ts` → `placement collision pre-check: occupied placement blocks a new request`<br>`promotions-dto-state.test.ts` → `OCCUPIED_PLACEMENT_STATUSES is exactly scheduled/active/paused (D-MERCH-12)` |
| CHK-PROMO-007 | covered | `promotions-billing.test.ts` → `pause/resume is idempotent, and lifecycle uses DB time to converge delayed scheduled rows to expired` |
| CHK-PROMO-008 | covered | `promotions-billing.test.ts` → `scheduled cancel is a single full refund under concurrency and replay does not refund twice`（30 并发 → cancelled=1/replayed=29，refund log 恰 1 条） |
| CHK-PROMO-009 | partial | `promotions-billing.test.ts` → `100 same-key adjustments replay one immutable refund decision; a new key conflicts`（points=40 路径：CAS 一次、refund log 唯一、新 key 409 `CAMPAIGN_ADJUSTMENT_ALREADY_DECIDED`）；points=0 分支无任何服务端用例 |
| CHK-PROMO-010 | partial | `catalog-merch-integration.test.ts` → `mounts public, merchant and admin merchandising routers at their frozen API paths`（admin 路由未认证 401）<br>`ranking-lifecycle.test.ts` → `requestManualRecompute rejects non-admin callers with 403 FORBIDDEN`；`server/src/modules/merchandising/promotions/routes.ts` 的 `requireAdmin`/`requireAdminMfa` 无用例覆盖 |
| CHK-PROMO-011 | covered | `promotions-billing.test.ts` → `100 concurrent approvals charge exactly once and never make balance negative`<br>同文件 → `scheduled cancel is a single full refund under concurrency and replay does not refund twice`<br>同文件 → `100 same-key adjustments replay one immutable refund decision; a new key conflicts` |
| CHK-PROMO-012 | partial | `src/components/merchandising/promotionCopy.test.ts` → `contains the frozen 不保证展示、点击或成交次数 wording`<br>`src/components/merchandising/PromotionPackagePicker.test.tsx` → `renders the no-guarantee disclosure and package price/duration/placement`；禁词表仅 `平台认证/官方认证/平台担保/质量保证`，无法币/CPM/CPC/竞价扫描 |
| CHK-PROMO-013 | covered | `promotions-idempotency.test.ts` → `create vector ["campaign-create-v1",42,7,null] → frozen SHA-256`<br>同文件 → `adjustment vector ["campaign-adjustment-v1",99,120,"排期调整"] → frozen SHA-256`<br>`promotions-campaign.test.ts` → `same key + different payload → 409 IDEMPOTENCY_KEY_REUSED; no second row` |
| CHK-PUBLIC-001 | covered | `promotions-billing.test.ts` → `public sponsored shelf returns only eligible active products with mandatory minimal disclosure`（merchant 转 suspended 后返回空）<br>`catalog-merch-integration.test.ts` → `mounts public, merchant and admin merchandising routers at their frozen API paths` |
| CHK-PUBLIC-002 | GAP | 实现在 `server/src/modules/merchandising/promotions/publicSponsored.ts`（`computeSponsoredBucket` / `computeRotationRank` / `computeSponsoredCacheKey`），全仓无任何测试文件 import 或断言这三个导出 |
| CHK-PUBLIC-003 | covered | `src/components/merchandising/SponsoredShelf.test.tsx` → `renders a forced 推广 disclosure for every card as visible DOM text (AC-MERCH-016)`<br>同文件 → `keeps disclosure icons decorative and renders host card content`<br>`e2e/merchandising-smoke.spec.ts` → `store home renders the sponsored shelf with a per-item 推广 disclosure and no organic double-count` |
| CHK-PUBLIC-004 | covered | `src/pages/StorePage.cmi.test.tsx` → `C: public shelves hydrate independently of the organic cursor; the organic card mounts its merchandising projection`（sponsored/editorial 商品从不出现为 `store-product-card`，badge strip 不含「推广」）<br>`e2e/merchandising-smoke.spec.ts` → `store home renders the sponsored shelf with a per-item 推广 disclosure and no organic double-count` |
| CHK-EDIT-001 | partial | `editorial-entitlements.test.ts` → `creates/patches/revokes with admin audit, uses DB lifecycle and hides inactive Product/Merchant`（3 条 AdminLog、`reason=运营调整`、scheduled→active→expired）<br>`src/components/merchandising/AdminEditorialManager.test.tsx` → `trims the reason and calls revokeFeature(101, trimmed)`；MFA 门禁本身无用例 |
| CHK-EDIT-002 | covered | `editorial-entitlements.test.ts` → `creates/patches/revokes with admin audit, uses DB lifecycle and hides inactive Product/Merchant`（Product inactive → shelf 空；draft 建精选直接 CONFLICT）<br>`src/components/merchandising/EditorialShelf.test.tsx` → `renders an independent shelf with the frozen 平台精选 label (AC-MERCH-018)` |
| CHK-ID-001 | covered | `ranking-compute-projection.test.ts` → `run=null → no-run fallback projection, hot block absent, never legacy isHot (AC-MERCH-008)`（`merchantId: null` → `platformOwned: true`）<br>`editorial-entitlements.test.ts` → `decorates editorial/partner identity even with no ranking run and hides both for an inactive merchant`（`platformOwned: false`）<br>`server/src/modules/merchant/product-is-hot.test.ts` → `unrelated unknown fields still fail as generic VALIDATION_ERROR, not FIELD_NOT_WRITABLE` |
| CHK-ID-002 | covered | `editorial-entitlements.test.ts` → `uses charge-log [start,end), subtracts current refunds, grants/extends idempotently and expires without deleting history`（30 并发 granted=1、阈值上调不延期、+31d 过期且历史行不删） |
| CHK-ID-003 | covered | `editorial-entitlements.test.ts` → `manual grant is 100-way safe, has bounded expiry/audit, minimal merchant DTO and immutable history`（AdminLog `授予平台合作伙伴权益` / `撤销平台合作伙伴权益`，`reason=合作结束`） |
| CHK-ID-004 | covered | `src/components/merchandising/MerchantPartnerMark.test.tsx` → `renders the frozen 平台合作伙伴 label as visible text`<br>同文件 → `exposes the frozen non-guarantee tooltip with the expected text`<br>同文件 → `does not use certification/guarantee words (CHK-ID-004 forbidden words)` |
| CHK-ID-005 | covered | `src/components/merchandising/badges.test.ts` → `orders badges as 平台自营 → 平台精选 → 热卖 regardless of input order`<br>同文件 → `never includes merchantPartner in the product badge strip (D-MERCH-19)`<br>`src/pages/StorePage.cmi.test.tsx` → `C: public shelves hydrate independently of the organic cursor; the organic card mounts its merchandising projection`（`['platform_owned','platform_pick','hot']` + partner mark 在商家区、badge strip 不含「推广」） |
| CHK-ID-006 | covered | `src/components/merchandising/BadgeMark.test.tsx` → `ignores unknown codes without a certification fallback (AC-MERCH-024)`<br>`src/components/merchandising/badges.test.ts` → `silently drops unknown codes and never falls back to a certification badge`<br>`src/components/merchandising/promotionCopy.test.ts` → `rejects unknown / null / undefined` |
| CHK-UI-001 | covered | `src/pages/StorePage.cmi.test.tsx` → `C: public shelves hydrate independently of the organic cursor; the organic card mounts its merchandising projection`（`merch-sponsored-shelf` / `merch-editorial-shelf` / organic grid 三分区，shelf 卡与 organic 卡 testid 互不冒充）<br>同文件 → `D: a store search query suppresses both public shelves — only the organic /products path is hit` |
| CHK-UI-002 | covered | `src/components/merchandising/PromotionPackagePicker.test.tsx` → `renders the no-guarantee disclosure and package price/duration/placement`<br>同文件 → `guards double submission while a request is in flight`<br>同文件 → `shows the selected package summary (套餐/展位/时长/价格)` |
| CHK-UI-003 | covered | `src/components/merchandising/MerchantCampaignPanel.test.tsx` → `renders every frozen status with its label, meta and timeline`<br>同文件 → `offers retry payment only for payment_failed via a two-step confirm`<br>同文件 → `fails closed for an unknown status: non-operable, neutral timeline, no invented semantics` |
| CHK-UI-004 | covered | `src/components/merchandising/AdminPromotionCampaignManager.test.tsx` → `approve PLACEMENT_OCCUPIED keeps the dialog with the exact collision copy (no success, no refresh), then a retry on the same dialog succeeds and refreshes`<br>同文件 → `reject: empty and whitespace-only reasons never call and show 请输入拒绝原因; a padded legal reason calls rejectCampaign(id, trimmed) once, closes and refreshes`<br>同文件 → `active refund success: points 120 + a padded reason on active 502 → createIdempotencyKey returns refund-key and adjustRefund(502, { points: 120, reason: trimmed }, refund-key) once, then 退款调整已完成。 role=status, dialog closed and current query refreshed` |
| CHK-UI-005 | covered | `src/components/merchandising/AdminEditorialManager.test.tsx` → `sends exact Next/Previous page queries while preserving the applied filters`<br>同文件 → `renders the admin-only internalReason inside the list table`<br>`src/components/merchandising/AdminEntitlementManager.test.tsx` → `applies merchantId=101 + active filter, pages with the filter preserved, and rejects an unsafe id without a list call` |
| CHK-UI-006 | partial | `src/components/merchandising/BadgeMark.test.tsx` → `exposes the badge strip to assistive tech with a labelled list`<br>`src/components/merchandising/MerchantPartnerMark.test.tsx` → `opens on keyboard focus (focus) and announces via aria-expanded`<br>`src/components/merchandising/SponsoredShelf.test.tsx` → `is keyboard/screen-reader accessible (labelled region, no axe violations)`；6 个 axe 断言全部 `rules: { 'color-contrast': { enabled: false } }`，无缩放用例 |
| CHK-ASSET-001 | Deferred | Deferred (AMD-CMI-012 §3.6) |
| CHK-ASSET-002 | Deferred | Deferred (AMD-CMI-012 §3.6) |
| CHK-ASSET-003 | Deferred | Deferred (AMD-CMI-012 §3.6) |
| CHK-ASSET-004 | Deferred | Deferred (AMD-CMI-012 §3.6) |
| CHK-ASSET-005 | Deferred | Deferred (AMD-CMI-012 §3.6) |
| CHK-ASSET-006 | Deferred | Deferred (AMD-CMI-012 §3.6) |
| CHK-SEC-001 | covered | `promotions-dto-state.test.ts` → `merchant campaign DTO carries the merchant-safe ledger totals but never leaks key/hash/point-log/adjustment/review internals`<br>`ranking-compute-projection.test.ts` → `frozen projection DTO fixtures only carry Spec §9 allowlist fields (MERCH-015 / CHK-SEC-001)`<br>`editorial-entitlements.test.ts` → `decorates editorial/partner identity even with no ranking run and hides both for an inactive merchant`（`not.toMatch(/sourceRef\|grantedBy\|verified\|guarantee/i)`） |
| CHK-SEC-002 | covered | `promotions-campaign.test.ts` → `merchant list is isolated: merchant B cannot see merchant A’s campaigns`<br>同文件 → `merchant cannot request another merchant’s product (404, no existence leak)`<br>`editorial-entitlements.test.ts` → `manual grant is 100-way safe, has bounded expiry/audit, minimal merchant DTO and immutable history` |
| CHK-SEC-003 | partial | `promotions-campaign.test.ts` → `admin reject → rejected with review fields + AdminLog; repeat reject 409; still uncharged`<br>`promotions-billing.test.ts` → `100 same-key adjustments replay one immutable refund decision; a new key conflicts`（PointLog refund 恒 1 条不可变）<br>`ranking-lifecycle.test.ts` → `requestManualRecompute is throttled by cadence (rate contract) and writes AdminLog on a real run`；`requireAdminMfa` 在 merch 路由无用例 |
| CHK-SEC-004 | partial | `promotions-campaign.test.ts` → `duplicate code → 409 PACKAGE_CODE_TAKEN (constraint name never leaks)`<br>`src/api/merchandising.test.ts` → `never echoes raw internal text or ids in messages`<br>`src/components/merchandising/AdminEditorialManager.test.tsx` → `never leaks the sensitive audit actor/user IDs (createdByUserId / revokedByUserId) into the DOM`；merch 路径的 token/email/余额/长 reason 日志脱敏无用例 |
| CHK-OPS-001 | partial | `ranking-lifecycle.test.ts` → `records bounded run outcomes and snapshot product gauge; rejects unknown labels`；`server/src/modules/merchandising/promotions/metrics.ts` 的 `promotionChargeTotal`/`promotionAdjustmentTotal`/`promotionCampaignTransitionTotal`/`promotionPublicItems` 与 entitlement 指标全仓无测试引用 |
| CHK-OPS-002 | partial | `ranking-lifecycle.test.ts` → `runRankingCronBatch runs when compute registered; start/stop are idempotent and lock is released after run`；`editorial/cron.ts` 与 `entitlements/cron.ts` 的 start/stop 幂等与多实例 lock 无用例 |
| CHK-PERF-001 | P1 | P1 |
| CHK-PERF-002 | P1 | 无 P95 延迟断言（组件/集成测试层不做延迟基线） |
| CHK-PERF-003 | covered | `ranking-compute-projection.test.ts` → `rejects a batch larger than PROJECTION_BATCH_LIMIT (bounded query, CHK-PERF-003)`<br>同文件 → `pinned run: single IN snapshot query (no N+1) and allowlist projection shape` |
| CHK-PERF-004 | Deferred | Deferred (§3.6) |
| CHK-QA-001 | partial | `ranking-compute-projection.test.ts` → `computeFilterHash matches the frozen SHA-256 vectors (CHK-HOT-011)` / `tie is stable by productId DESC (D-MERCH-05)` / `decodeOrganicCursor round-trips the frozen cursors`<br>`src/components/merchandising/promotionCopy.test.ts` → `accepts all 8 frozen statuses`<br>`src/components/merchandising/forbiddenWords.test.tsx` → `combined fixture (all primitives at once)`；rotation 单测缺失（见 CHK-PUBLIC-002） |
| CHK-QA-002 | covered | `ranking-lifecycle.test.ts` → `is exclusive across two dedicated connections, and is released on release()`<br>同文件 → `creates running (short tx A), writes snapshots + completes (tx B), freezes config and DB time`<br>同文件 → `commit failure (CHECK violation in snapshot write) → whole tx B rolled back, run failed, previous intact` |
| CHK-QA-003 | covered | `promotions-billing.test.ts` → `100 concurrent approvals charge exactly once and never make balance negative`<br>同文件 → `scheduled cancel is a single full refund under concurrency and replay does not refund twice`<br>同文件 → `concurrent approval of two pending rows relies on the placement constraint and rolls one charge back` |
| CHK-QA-004 | partial | `promotions-campaign.test.ts` → `client cannot override price/placement/duration (unknown body fields are not accepted)`<br>`catalog-merch-integration.test.ts` → `mounts public, merchant and admin merchandising routers at their frozen API paths`<br>`promotions-dto-state.test.ts` → `admin campaign DTO exposes review and billing summaries but no key/hash/point-log fields`；merchant 持票访问 admin 端点的 403 用例缺失 |
| CHK-QA-005 | covered | `e2e/merchandising-smoke.spec.ts` → `admin creates a package, merchant requests its own active product, admin approves → active + charged` / `store home renders the sponsored shelf with a per-item 推广 disclosure and no organic double-count` / `merchant campaign panel shows the active campaign with the charged timeline state`（3 用例） |
| CHK-QA-006 | partial | `src/components/merchandising/SponsoredShelf.test.tsx` → `renders a forced 推广 disclosure for every card as visible DOM text (AC-MERCH-016)` / `is keyboard/screen-reader accessible (labelled region, no axe violations)`<br>`src/components/merchandising/forbiddenWords.test.tsx` → `combined fixture (all primitives at once)`；visual/assets/bundle `Deferred` |
| CHK-QA-007 | covered | 回归套件全绿（suite 级）：`server/src/__tests__/orders.test.ts`、`server/src/__tests__/refund-inventory-policy.test.ts`、`server/src/modules/products/public-fields.test.ts` → `never exposes fixedContent and includes stockMode/deliveryMode`、`catalog-merch-integration.test.ts` → `keeps an inactive historical category in the DTO but excludes it from an active code filter` |
| CHK-QA-008 | covered | 通知套件全绿（suite 级）：`server/src/modules/notifications/__tests__/integration.test.ts`、`dispatcher.test.ts`、`templates.test.ts`、`service.test.ts`；owned-file 零冲突由最终 HEAD git diff 审计确认 |
| CHK-REL-001 | runbook | runbook 确认(§2.4) |
| CHK-REL-002 | runbook | runbook 确认(§2.4) |
| CHK-REL-003 | runbook | runbook 确认(§2.4) |
| CHK-REL-004 | runbook | runbook 文档条目 |

## GAP 清单

- **CHK-PUBLIC-002（GAP）** — 缺 `computeSponsoredBucket` / `computeRotationRank` 的确定性与公平性断言：同一 10 分钟窗口内排序稳定、跨窗口发生轮换、以及 `listSponsoredItems` 在 60s 缓存期内不产生每请求 DB 写。三个纯函数导出目前零测试引用。
- **CHK-PROMO-009（partial）** — 缺 `adjustCampaignRefund(..., { points: 0, reason })` 的用例：应断言 0 积分调整仍写下不可变决定（`adjustmentDecidedAt` CAS 生效、同 key 重放、新 key 409），但**不**产生任何 `type: 'refund'` 的 PointLog 行。
- **CHK-PROMO-010（partial）** — 缺「已认证 merchant 令牌打 admin refund/transition 端点得 403」的用例；现有证据只到未认证 401 与 ranking `requestManualRecompute` 的 403。`requireAdminMfa` 对 merch admin 路由的门禁效果同样无断言。
- **CHK-PROMO-012（partial）** — 缺法币/CPM/CPC/竞价/收益承诺的负向扫描：现有禁词表只有 `平台认证/官方认证/平台担保/质量保证`，未覆盖计费模型相关措辞。
- **CHK-HOT-011（partial）** — 缺 `buildProductListCacheKey` 的用例：应断言 `rankingRunId` 与 filterHash 进入缓存 key，使得新 run 完成后不会命中旧 run 的缓存页。
- **CHK-EDIT-001（partial）** — 缺 editorial admin 路由的 MFA 门禁断言（缺 MFA 声明的 admin token 应被挡下）。
- **CHK-SEC-003（partial）** — 缺 merch admin mutation 的 MFA 强制断言；PointLog 不可变仅由「refund 行恒为 1」间接证明，无显式 update/delete 拒绝用例。
- **CHK-SEC-004（partial）** — 缺 merch 错误/日志/trace 的脱敏断言（token、email、余额数值、超长 reason 截断）；现有证据只覆盖约束名与 actor id 不泄露。
- **CHK-OPS-001（partial）** — 缺 charge / transition / public-sponsored / entitlement 四组指标的记录与 label 有界断言（仅 ranking run 指标有测试）。
- **CHK-OPS-002（partial）** — 缺 `editorial/cron.ts` 与 `entitlements/cron.ts` 的 start/stop 幂等、多实例 lock、无 timer 泄漏断言。
- **CHK-UI-006（partial）** — 缺对比度与缩放证据：全部 6 处 axe 断言显式禁用 `color-contrast`，且无 200%/400% 缩放布局用例。
- **CHK-QA-001（partial）** — 单测清单中 rotation 一项缺失，同 CHK-PUBLIC-002。
- **CHK-QA-004（partial）** — auth 边界集成未闭环，同 CHK-PROMO-010。
- **CHK-QA-006（partial）** — disclosure/a11y 有证据，visual regression 与 asset/bundle Gate 按 §3.6 Deferred，故该项无法在本 HEAD 全绿。
- **CHK-PERF-002（P1）** — 无 organic P95 退化基线与 sponsored/editorial 300ms 断言；按 AMD-CMI-012 归 P1，不在本次证据采集范围。

---

## 3. 表外节次

- catalog §1(DOC/PAR)与 §2(MIG):由 C5 台账出具(foundation Gate 脚本三套 fixture + `git merge-base --is-ancestor` 命令序列)。
- catalog §9(QA/REL)按 AMD-CMI-012 §4.1:QA 行引用上表,REL 行 runbook 确认。
- 两份 checklist 的 Final Gate 节(§10/§12):C5 在 merge develop 后的最终 HEAD 一次出具。
